/** Re-export, so the checker imports the kit without pulling the DOM in behind it. */
export { AXES, DEFAULT_AXES, SKETCHES, VOICINGS, buildKit, buildSound, noteName, rootHzFor } from './audio/kit.js';
export type { Axes, KitSpec, Sketch, Voicing } from './audio/kit.js';
